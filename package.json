{
  "name": "trip-planner",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm ci --include=dev && npm run build && cd ../server && npm ci --include=dev && npx prisma generate && npm run build",
    "start": "cd server && npx prisma db push --accept-data-loss && npx prisma db seed && node dist/index.js",
    "db:seed": "cd server && npm run db:seed",
    "db:migrate": "cd server && npm run db:migrate"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
